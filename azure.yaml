# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json

name: GitTasker
metadata:
    template: azd-init@1.21.3
services:
    GitTasker:
        project: ./generated-spring
        host: containerapp
        language: java
        docker:
            path: ../Dockerfile
            context: .
resources:
    GitTasker:
        type: host.containerapp
        port: 8080
